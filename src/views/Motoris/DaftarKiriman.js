import React from "react"
import { ScrollView, View } from "react-native"
import { Text } from "_atoms"

const DaftarKiriman = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>Fitur ini belum tersedia</Text>
    </View>
  )
}

export default DaftarKiriman
